import { DataRightsScreen } from "@/features/user/screens/DataRightsScreen";

export const metadata = {
  title: "Privacidade e Dados",
};

export default function DataRightsPage() {
  return <DataRightsScreen />;
}
